{
  "name": "icon-config-manager",
  "version": "1.0.0",
  "description": "Icon Configuration Manager with optimized UI and automated publishing",
  "main": "index.html",
  "scripts": {
    "start": "npx http-server -p 8080 -c-1",
    "build": "echo 'Build completed'",
    "deploy": "npm run build && npm run push",
    "push": "git add . && git commit -m 'chore: auto deploy update' && git push origin main"
  },
  "keywords": [
    "icon",
    "config",
    "manager",
    "ui"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
